<?xml version="1.0" encoding="utf-8"?>
<!--
    AUDIO/OTHER ALBUM CARD - Redesigned to match the premium dark glassmorphism card style:
    [CoverSquare(Icon + Count)] | [FolderName + FullPath] | [>]
-->
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="12dp"
    android:layout_marginVertical="5dp"
    android:foreground="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true"
    app:cardCornerRadius="16dp"
    app:cardElevation="3dp"
    app:cardBackgroundColor="@color/colorCardBg">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:padding="12dp">

        <!-- ===== LEFT: Colored square folder box (icon + count) ===== -->
        <LinearLayout
            android:id="@+id/imageLayout"
            android:layout_width="72dp"
            android:layout_height="80dp"
            android:orientation="vertical"
            android:gravity="center"
            android:background="@drawable/bg_card_audios"
            android:padding="6dp"
            android:layout_marginEnd="12dp">

            <ImageView
                android:id="@+id/iv_folder_icon"
                android:layout_width="26dp"
                android:layout_height="26dp"
                android:src="@drawable/ic_cat_audio"
                android:tint="#FFFFFF"
                android:layout_marginBottom="4dp"/>

            <TextView
                android:id="@+id/tv_folder2"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:text="0\nAudios"
                android:textColor="#FFFFFF"
                android:textSize="11sp"
                android:textStyle="bold"/>
        </LinearLayout>

        <!-- ===== MIDDLE: Folder Name + Full Path ===== -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="80dp"
            android:layout_weight="1"
            android:orientation="vertical"
            android:gravity="center_vertical">

            <!-- Dynamic Folder Name (e.g. Music, WhatsApp Audio) -->
            <TextView
                android:id="@+id/tvFolderName"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Folder Name"
                android:textColor="@color/colorTextPrimary"
                android:textSize="15sp"
                android:textStyle="bold"
                android:maxLines="1"
                android:ellipsize="end"/>

            <!-- Dimmed Raw Directory Path -->
            <TextView
                android:id="@+id/filePath"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="/storage/emulated/0/Folder"
                android:textColor="@color/colorTextSecondary"
                android:textSize="11sp"
                android:maxLines="1"
                android:ellipsize="end"
                android:layout_marginTop="5dp"/>
        </LinearLayout>

        <!-- ===== RIGHT: Chevron Arrow ===== -->
        <ImageView
            android:id="@+id/im_arrow"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_marginStart="8dp"
            android:src="@drawable/ic_chevron_right"
            app:tint="@color/colorTextHint"/>

    </LinearLayout>
</androidx.cardview.widget.CardView>
